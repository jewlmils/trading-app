<h1>Search Stocks</h1>

<%= form_tag(stocks_path, method: :get) do %>
  <div class="field">
    <%= text_field_tag :search, params[:search], placeholder: "Search stocks by symbol..." %>
    <%= submit_tag "Search", class: "btn btn-primary" %>
  </div>
<% end %>