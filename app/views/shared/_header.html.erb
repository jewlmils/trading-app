<% if current_trader.present? || current_admin.present? %>
<nav class="navbar bg-gray24 border-slate-950 w-full h-16 flex items-center justify-between px-6 py-4 drop-shadow-lg">
    <div class="flex items-center">
      <%= link_to 'Trading App', root_path, class: "txt-gradient text-2xl font-bold text-white" %> 
    </div>
    
    <div class="w-max flex justify-center items-center text-sky-50">
      <% if current_trader.present? %>
        <%= link_to destroy_trader_session_path, data: { turbo_method: :delete, turbo_confirm: "Log out?" }, class: "actions btn--subv1" do %>
          <div class="flex items-center gap-2">
            <p>Hello, <%= current_trader.first_name %>!</p>
            <i class="fa-solid fa-arrow-right-from-bracket"></i>
            <p class="hidden sm:inline-flex">
              Logout
            </p>
          </div>
        <% end %>
      <% elsif current_admin.present? %>
        <%= link_to destroy_admin_session_path, data: { turbo_method: :delete, turbo_confirm: "Log out?" }, class: "actions btn--subv1" do %>
          <div class="flex items-center gap-2">
            <p>Hello, Admin👋</p>
            <i class="fa-solid fa-arrow-right-from-bracket"></i>
            <p class="hidden sm:inline-flex">
              Logout
            </p>
          </div>
        <% end %>
      <% end %>
    </div>
</nav>
<% end %>



