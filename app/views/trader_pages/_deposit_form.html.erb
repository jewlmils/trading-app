<%= form_tag(trader_trader_dashboard_path, method: :post) do %>
  <div class="field">
    <%= label_tag 'amount', 'Deposit Amount' %>
    <%= number_field_tag 'amount', nil, step: "0.01" %>
  </div>

  <div class="actions">
    <%= submit_tag "Deposit" %>
  </div>
<% end %>
